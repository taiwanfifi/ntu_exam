# 電磁學教學講義 07：電磁波

---

## 🔰 **本章基礎觀念（零基礎必讀）**

### 電磁波是什麼？

上一章我們看到：
- 變化的磁場 → 產生電場（法拉第定律）
- 變化的電場 → 產生磁場（安培-馬克士威定律）

這兩件事接力起來會發生什麼？**電生磁、磁再生電、電再生磁......** 像骨牌一樣一路往前推進——這就是**電磁波**！

光、微波、Wi-Fi 訊號、手機訊號、X 光——全部都是電磁波，只是頻率不同。

| 類型 | 頻率範圍 | 日常例子 |
|------|---------|---------|
| 無線電波 | kHz ~ GHz | AM/FM 收音機、Wi-Fi |
| 微波 | 1 ~ 300 GHz | 微波爐、5G、雷達 |
| 紅外線 | 300 GHz ~ 400 THz | 遙控器、夜視鏡 |
| 可見光 | 400 ~ 750 THz | 你正在用的「光」 |
| 紫外線 | 750 THz ~ 30 PHz | 防曬、殺菌 |
| X 射線 | 30 PHz ~ 30 EHz | 醫學影像 |

---

## 一、從馬克士威方程推導波動方程

### 1.1 推導過程

在**無源自由空間**（$\rho_v = 0$, $\vec{J} = 0$）中，馬克士威方程為：

$$\nabla \times \vec{E} = -\mu_0\frac{\partial \vec{H}}{\partial t} \quad \cdots (1)$$

$$\nabla \times \vec{H} = \varepsilon_0\frac{\partial \vec{E}}{\partial t} \quad \cdots (2)$$

對 (1) 兩邊取旋度：

$$\nabla \times (\nabla \times \vec{E}) = -\mu_0\frac{\partial}{\partial t}(\nabla \times \vec{H})$$

左邊用向量恆等式：$\nabla \times (\nabla \times \vec{E}) = \nabla(\nabla \cdot \vec{E}) - \nabla^2\vec{E}$

因為 $\nabla \cdot \vec{E} = 0$（自由空間無電荷），所以左邊 $= -\nabla^2\vec{E}$。

右邊代入 (2)：$-\mu_0\varepsilon_0\frac{\partial^2 \vec{E}}{\partial t^2}$

$$\boxed{\nabla^2\vec{E} = \mu_0\varepsilon_0\frac{\partial^2 \vec{E}}{\partial t^2}}$$

這就是**電場的波動方程（Wave Equation）**！

同理可得磁場的波動方程：

$$\nabla^2\vec{H} = \mu_0\varepsilon_0\frac{\partial^2 \vec{H}}{\partial t^2}$$

> 白話：這個方程說「空間中的電場波紋會隨著時間向外傳播」——就像丟石頭到水面上，水波向外擴散。

### 1.2 波速

比較標準波動方程 $\nabla^2 f = \frac{1}{v^2}\frac{\partial^2 f}{\partial t^2}$：

$$v = \frac{1}{\sqrt{\mu_0\varepsilon_0}} = \frac{1}{\sqrt{4\pi \times 10^{-7} \times 8.854 \times 10^{-12}}}$$

$$= \frac{1}{\sqrt{1.113 \times 10^{-17}}} = \frac{1}{3.336 \times 10^{-9}} \approx 3 \times 10^8 \text{ m/s}$$

這就是**光速 c**！馬克士威在 1865 年算出來的時候，他就知道**光是電磁波**。

在一般介質中：

$$v = \frac{1}{\sqrt{\mu\varepsilon}} = \frac{c}{\sqrt{\mu_r\varepsilon_r}} = \frac{c}{n}$$

其中 $n = \sqrt{\mu_r\varepsilon_r}$ 是**折射率（Refractive Index）**。

---

## 二、均勻平面波（Uniform Plane Wave）

### 2.1 定義

最簡單的電磁波解：電場和磁場只在一個方向上變化，且在垂直於傳播方向的平面上是均勻的。

假設波沿 $+z$ 方向傳播，E 只有 x 分量：

$$\vec{E}(z,t) = E_0\cos(\omega t - \beta z)\,\hat{x}$$

對應的磁場（由馬克士威方程推得）：

$$\vec{H}(z,t) = \frac{E_0}{\eta}\cos(\omega t - \beta z)\,\hat{y}$$

### 2.2 重要參數

| 參數 | 符號 | 公式 | 物理意義 |
|------|------|------|---------|
| 角頻率 | $\omega$ | $2\pi f$ | 每秒振盪幾弧度 |
| 頻率 | $f$ | | 每秒振盪幾次 |
| 相位常數 | $\beta$ | $\omega\sqrt{\mu\varepsilon} = \omega/v = 2\pi/\lambda$ | 每公尺的相位變化 |
| 波長 | $\lambda$ | $v/f = 2\pi/\beta$ | 一個週期的空間長度 |
| 波速 | $v$ | $1/\sqrt{\mu\varepsilon} = \omega/\beta = f\lambda$ | 等相位面的移動速度 |
| 波阻抗 | $\eta$ | $\sqrt{\mu/\varepsilon}$ | E 和 H 的比值 |

**真空中的數值：**
- $v = c = 3 \times 10^8$ m/s
- $\eta_0 = \sqrt{\mu_0/\varepsilon_0} = 120\pi \approx 377\;\Omega$

### 📝 例題 1：均勻平面波的基本計算

**題目：** 在自由空間中，頻率 f = 100 MHz 的電磁波。求 (a) 波長，(b) 相位常數，(c) 波阻抗。

**解：**

**(a) 波長：**
$$\lambda = \frac{c}{f} = \frac{3 \times 10^8}{100 \times 10^6} = 3 \text{ m}$$

**(b) 相位常數：**
$$\beta = \frac{2\pi}{\lambda} = \frac{2\pi}{3} \approx 2.094 \text{ rad/m}$$

**(c) 波阻抗：**
$$\eta_0 = 120\pi \approx 377\;\Omega$$

---

### 2.3 重要性質

1. **E 和 H 互相垂直**，且都垂直於傳播方向（**TEM 波，橫電磁波**）
2. **E 和 H 同相位**（在無損介質中），峰值同時出現
3. **E/H = η**（波阻抗）
4. 傳播方向 = $\hat{E} \times \hat{H}$

### 📝 例題 2：從 E 求 H

**題目：** 在 $\varepsilon_r = 4$, $\mu_r = 1$ 的介質中，$\vec{E} = 50\sin(10^8 t - 2z/3)\hat{y}$ V/m。求 $\vec{H}$。

**解：**

波阻抗：$\eta = \eta_0/\sqrt{\varepsilon_r} = 377/\sqrt{4} = 188.5\;\Omega$

傳播方向：$+\hat{z}$（因為 $\omega t - \beta z$ 代表 +z 傳播）

$$\hat{z} = \hat{E} \times \hat{H} = \hat{y} \times \hat{H}$$

$\hat{H}$ 必須是 $-\hat{x}$？不，$\hat{y} \times (-\hat{x}) = \hat{z}$？

$\hat{y} \times (-\hat{x}) = -(\hat{y} \times \hat{x}) = -(-\hat{z}) = \hat{z}$ ✓

等等，更直覺的方式：$\hat{z} = \hat{y} \times \hat{H}$，所以 $\hat{H} = -\hat{x}$。

但這不對，讓我用正確方向：

$\hat{E} \times \hat{H}$ = 傳播方向，所以 $\hat{y} \times \hat{H} = \hat{z}$

$\hat{H} = ?$ → 需要 $\hat{y} \times \hat{H} = \hat{z}$

如果 $\hat{H} = \hat{x}$：$\hat{y} \times \hat{x} = -\hat{z}$ ✗

如果 $\hat{H} = -\hat{x}$：$\hat{y} \times (-\hat{x}) = \hat{z}$ ✓

$$\vec{H} = -\frac{50}{188.5}\sin(10^8 t - 2z/3)\hat{x} = -0.2653\sin(10^8 t - 2z/3)\hat{x} \text{ A/m}$$

或寫成 $\vec{H} = \frac{50}{188.5}\sin(10^8 t - 2z/3)(-\hat{x})$ A/m

---

## 三、極化（Polarization）

### 3.1 定義

極化描述的是電場向量 $\vec{E}$ 的尖端隨時間在空間中劃出的軌跡。

### 3.2 三種極化

**1. 線極化（Linear Polarization）：**

E 向量沿一條直線振動。

$$\vec{E} = E_0\cos(\omega t - \beta z)\hat{x}$$

或

$$\vec{E} = (E_1\hat{x} + E_2\hat{y})\cos(\omega t - \beta z)$$

（兩個分量同相位或差 180°）

**2. 圓極化（Circular Polarization）：**

E 向量的尖端劃出一個圓。

$$\vec{E} = E_0[\cos(\omega t - \beta z)\hat{x} \pm \sin(\omega t - \beta z)\hat{y}]$$

- "+" → 左旋圓極化（LHCP）
- "-" → 右旋圓極化（RHCP）

兩個分量**等振幅**且**相位差 90°**。

**3. 橢圓極化（Elliptical Polarization）：**

E 向量的尖端劃出一個橢圓。這是最一般的情況。

$$\vec{E} = E_1\cos(\omega t - \beta z)\hat{x} + E_2\cos(\omega t - \beta z + \delta)\hat{y}$$

（兩個分量振幅不等或相位差不是 0°/90°/180°）

### 📝 例題 3：判斷極化類型

**題目：** $\vec{E} = 3\cos(\omega t - \beta z)\hat{x} + 3\sin(\omega t - \beta z)\hat{y}$ V/m。這是什麼極化？

**解：**

$E_x = 3\cos(\omega t - \beta z)$，$E_y = 3\sin(\omega t - \beta z) = 3\cos(\omega t - \beta z - 90°)$

- 兩個分量振幅相等：$|E_x| = |E_y| = 3$ ✓
- 相位差 = $-90°$

等振幅 + 90° 相位差 → **圓極化**。

因為 $\sin$ 比 $\cos$ 晚了 90°（$E_y$ 落後 $E_x$），且我們用的是 $\cos(\omega t - \beta z)$ 的形式，這是**右旋圓極化（RHCP）**。

在 z = 0 處：t = 0 時 E 指向 $+\hat{x}$，t 增加時 E 轉向 $+\hat{y}$，從 $+z$ 方向看去是逆時針旋轉。（注意：RHCP/LHCP 的定義因課本而異，請以你的課本為準。）

---

## 四、波印亭向量（Poynting Vector）

### 4.1 定義

$$\vec{S} = \vec{E} \times \vec{H} \quad \text{(W/m}^2\text{)}$$

$\vec{S}$ 代表**電磁波攜帶的瞬時功率密度**——每秒鐘通過單位面積的能量。

> 白話：波印亭向量告訴你「電磁能量往哪裡流、流多快」。

### 4.2 平均功率密度

因為 $\cos^2$ 的平均值是 1/2：

$$\vec{S}_{avg} = \frac{1}{2}\text{Re}[\vec{E}_s \times \vec{H}_s^*] = \frac{|E_0|^2}{2\eta}\hat{a}_n$$

其中 $\hat{a}_n$ 是傳播方向。

### 📝 例題 4：功率密度計算

**題目：** 自由空間中 $\vec{E} = 100\cos(10^9 t - \beta z)\hat{x}$ V/m。求 (a) 瞬時波印亭向量，(b) 平均功率密度。

**解：**

$H_0 = E_0/\eta_0 = 100/377 = 0.2653$ A/m

$\vec{H} = 0.2653\cos(10^9 t - \beta z)\hat{y}$ A/m

**(a) 瞬時波印亭向量：**

$$\vec{S} = \vec{E} \times \vec{H} = 100 \times 0.2653 \times \cos^2(10^9 t - \beta z)(\hat{x} \times \hat{y})$$

$$= 26.53\cos^2(10^9 t - \beta z)\hat{z} \text{ W/m}^2$$

**(b) 平均功率密度：**

$$S_{avg} = \frac{E_0^2}{2\eta_0} = \frac{100^2}{2 \times 377} = \frac{10000}{754} = 13.26 \text{ W/m}^2$$

---

## 五、損耗介質中的傳播

### 5.1 複數波數

在有損耗的介質中（$\sigma \neq 0$），馬克士威方程導出的波數變成複數：

$$\gamma = \alpha + j\beta = j\omega\sqrt{\mu\varepsilon}\sqrt{1 - j\frac{\sigma}{\omega\varepsilon}}$$

或更明確地：

$$\gamma = j\omega\sqrt{\mu\varepsilon_c}$$

其中 $\varepsilon_c = \varepsilon' - j\varepsilon'' = \varepsilon - j\sigma/\omega$ 是**複數介電常數**。

- $\alpha$：**衰減常數**（Attenuation Constant），Np/m
- $\beta$：**相位常數**（Phase Constant），rad/m

電場在傳播時衰減：

$$\vec{E} = E_0 e^{-\alpha z}\cos(\omega t - \beta z)\hat{x}$$

### 5.2 趨膚深度（Skin Depth）

$$\delta = \frac{1}{\alpha}$$

白話：電磁波穿進材料後，振幅衰減到原來的 $1/e$（≈ 37%）的深度。

> 這就是為什麼高頻電流只在導體表面流動——叫做**趨膚效應（Skin Effect）**。頻率越高，趨膚深度越淺，電流越「擠」在表面。

### 📝 例題 5：趨膚深度

**題目：** 銅（$\sigma = 5.8 \times 10^7$ S/m, $\mu_r = 1$）在 f = 1 GHz 時的趨膚深度。

**解：**

在良導體近似下（$\sigma \gg \omega\varepsilon$，銅絕對滿足）：

$$\alpha = \beta = \sqrt{\frac{\omega\mu\sigma}{2}} = \sqrt{\pi f \mu_0 \sigma}$$

$$= \sqrt{\pi \times 10^9 \times 4\pi \times 10^{-7} \times 5.8 \times 10^7}$$

$$= \sqrt{\pi \times 10^9 \times 1.2566 \times 10^{-6} \times 5.8 \times 10^7}$$

$$= \sqrt{\pi \times 7.288 \times 10^{10}}$$

$$= \sqrt{2.289 \times 10^{11}} = 4.785 \times 10^5 \text{ Np/m}$$

$$\delta = \frac{1}{\alpha} = \frac{1}{4.785 \times 10^5} = 2.09 \times 10^{-6} \text{ m} \approx 2.09\;\mu\text{m}$$

> 只有 2 微米！所以在 1 GHz，銅線中的電流幾乎全部集中在表面 2 μm 以內。這就是為什麼高頻電路中導體要鍍金或鍍銀——因為只有表面的導電性重要。

---

### 5.3 良導體近似 vs 良絕緣體近似

**判斷標準：** 損耗正切（Loss Tangent）

$$\tan\delta_l = \frac{\sigma}{\omega\varepsilon}$$

| 條件 | 類型 | 特徵 |
|------|------|------|
| $\sigma/\omega\varepsilon \gg 1$ | 良導體 | $\alpha \approx \beta \approx \sqrt{\pi f\mu\sigma}$，$\delta$ 很小 |
| $\sigma/\omega\varepsilon \ll 1$ | 良絕緣體（低損耗介質） | $\alpha \approx \frac{\sigma}{2}\sqrt{\mu/\varepsilon}$，$\beta \approx \omega\sqrt{\mu\varepsilon}$ |
| 其他 | 一般介質 | 用完整公式 |

### 5.4 良導體中的波阻抗

$$\eta_c = \frac{1+j}{\sigma\delta} = (1+j)\sqrt{\frac{\omega\mu}{2\sigma}} = (1+j)\sqrt{\frac{\pi f\mu}{\sigma}}$$

> 注意：良導體的波阻抗是**複數**，且相位角為 45°——E 和 H 不再同相！

### 5.5 低損耗介質中的近似

$$\alpha \approx \frac{\sigma}{2}\sqrt{\frac{\mu}{\varepsilon}}$$

$$\beta \approx \omega\sqrt{\mu\varepsilon}$$

$$\eta \approx \sqrt{\frac{\mu}{\varepsilon}}\left(1 + j\frac{\sigma}{2\omega\varepsilon}\right)$$

---

## 六、複數表示法（Phasor）

為了方便處理正弦穩態的電磁波，我們用**相量（Phasor）**表示法。

瞬時場和相量的關係：

$$\vec{E}(z,t) = \text{Re}[\vec{E}_s(z) e^{j\omega t}]$$

例如 $\vec{E}(z,t) = E_0\cos(\omega t - \beta z)\hat{x}$ 對應的相量：

$$\vec{E}_s = E_0 e^{-j\beta z}\hat{x}$$

用相量表示後，$\partial/\partial t$ 變成 $j\omega$，微分方程變成代數方程，計算大幅簡化！

---

## 七、題型鑑別

| 看到什麼 | 用什麼方法 |
|---------|----------|
| 「推導波動方程」 | 對法拉第取旋度 + 代入安培 |
| 「求波長/頻率/波速/β」 | $\lambda = v/f$, $\beta = 2\pi/\lambda$, $v = 1/\sqrt{\mu\varepsilon}$ |
| 「從 E 求 H」 | $H = E/\eta$，方向用 $\hat{a}_n = \hat{E} \times \hat{H}$ |
| 「判斷極化」 | 看兩分量的振幅和相位差 |
| 「求功率密度」 | 波印亭向量 $\vec{S} = \vec{E} \times \vec{H}$，平均 $= E_0^2/(2\eta)$ |
| 「有損耗的介質」 | 用複數波數 $\gamma = \alpha + j\beta$ |
| 「趨膚深度」 | $\delta = 1/\alpha$ |
| 「良導體 or 良絕緣體？」 | 看 $\sigma/\omega\varepsilon$ 遠大於或遠小於 1 |

---

## 關鍵術語表

| 中文 | 英文 | 白話解釋 | 例子 |
|------|------|---------|------|
| 波動方程 | Wave Equation | 描述波如何在空間傳播的方程 | ∇²E = με ∂²E/∂t² |
| 均勻平面波 | Uniform Plane Wave | 最簡單的電磁波模型 | 遠場的近似 |
| 波阻抗 | Intrinsic Impedance | E/H 的比值 | 真空中 377 Ω |
| 相位常數 | Phase Constant (β) | 每公尺的相位變化量 | 2π/λ |
| 衰減常數 | Attenuation Constant (α) | 振幅衰減的速率 | 良導體中很大 |
| 趨膚深度 | Skin Depth (δ) | 波衰減到 1/e 的深度 | 銅在 1GHz 約 2μm |
| 波印亭向量 | Poynting Vector | 電磁能量流動方向和速率 | S = E × H |
| 極化 | Polarization | E 向量尖端的軌跡 | 線極化、圓極化 |
| 損耗正切 | Loss Tangent | 衡量介質損耗程度 | σ/(ωε) |
| 相量 | Phasor | 正弦穩態的複數表示 | E₀e^(-jβz) |

---

## ✅ 自我檢測

### Q1：基本計算
在 $\varepsilon_r = 9$, $\mu_r = 1$ 的無損介質中，電磁波的速度和波阻抗為何？

<details><summary>點擊展開答案</summary>

$$v = \frac{c}{\sqrt{\varepsilon_r}} = \frac{3 \times 10^8}{\sqrt{9}} = \frac{3 \times 10^8}{3} = 10^8 \text{ m/s}$$

$$\eta = \frac{\eta_0}{\sqrt{\varepsilon_r}} = \frac{377}{3} \approx 125.7\;\Omega$$

</details>

### Q2：從 E 求 H
$\vec{E} = 20\sin(2\pi \times 10^9 t + 40z)\hat{y}$ V/m（自由空間中）。求 $\vec{H}$。

<details><summary>點擊展開答案</summary>

$\omega t + \beta z$ → 波沿 $-z$ 方向傳播。

$H_0 = E_0/\eta_0 = 20/377 = 0.053$ A/m

傳播方向 $-\hat{z} = \hat{y} \times \hat{H}$

需要 $\hat{y} \times \hat{H} = -\hat{z}$，所以 $\hat{H} = \hat{x}$（因為 $\hat{y} \times \hat{x} = -\hat{z}$ ✓）

$$\vec{H} = 0.053\sin(2\pi \times 10^9 t + 40z)\hat{x} \text{ A/m}$$

</details>

### Q3：極化
$\vec{E} = 5\cos(\omega t - \beta z)\hat{x} + 10\cos(\omega t - \beta z)\hat{y}$。這是什麼極化？

<details><summary>點擊展開答案</summary>

兩個分量**同相位**但振幅不等（5 和 10）。

同相位 → **線極化**。

極化方向和 x 軸的夾角：$\theta = \tan^{-1}(10/5) = \tan^{-1}(2) \approx 63.4°$。

</details>

### Q4：波印亭
太陽光到達地球表面的平均功率密度約為 1000 W/m²。求電場振幅。

<details><summary>點擊展開答案</summary>

$$S_{avg} = \frac{E_0^2}{2\eta_0}$$

$$E_0 = \sqrt{2\eta_0 S_{avg}} = \sqrt{2 \times 377 \times 1000} = \sqrt{754000} \approx 868 \text{ V/m}$$

</details>

### Q5：趨膚深度
海水（$\sigma = 4$ S/m, $\varepsilon_r = 81$, $\mu_r = 1$）在 f = 1 MHz 時是良導體嗎？趨膚深度是多少？

<details><summary>點擊展開答案</summary>

$$\frac{\sigma}{\omega\varepsilon} = \frac{4}{2\pi \times 10^6 \times 81 \times 8.854 \times 10^{-12}} = \frac{4}{4.506 \times 10^{-3}} = 888 \gg 1$$

是良導體！

$$\delta = \frac{1}{\sqrt{\pi f\mu_0\sigma}} = \frac{1}{\sqrt{\pi \times 10^6 \times 4\pi \times 10^{-7} \times 4}}$$

$$= \frac{1}{\sqrt{16\pi^2 \times 10^{-1}}} = \frac{1}{\sqrt{15.79}} = \frac{1}{3.974} = 0.252 \text{ m} \approx 25.2 \text{ cm}$$

海水在 1 MHz 的趨膚深度僅 25 cm！這就是為什麼潛水艇要用極低頻（ELF, 幾十 Hz）通訊。

</details>

---

> **上一篇：** [em_06 電磁感應與馬克士威方程](em_06_電磁感應與馬克士威方程.md)
> **下一篇：** [em_08 電磁波反射與透射](em_08_電磁波反射與透射.md)
